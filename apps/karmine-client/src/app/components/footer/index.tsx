import { Link, Route, Routes } from "react-router-dom"
import { useStyles } from "./styles"

export const Footer = () => {
  const { test } = useStyles()
	return (
    <>
      <div style={test}>
      </div>
    </>
    )
}